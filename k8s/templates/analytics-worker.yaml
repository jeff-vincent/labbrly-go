apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-worker
  labels:
    app: analytics-worker
spec:
  selector:
    matchLabels:
      app: analytics-worker
  replicas: 1
  template:
    metadata:
      labels:
        app: analytics-worker
    spec:
      containers:
        - name: analytics-worker
          image: {{ .Values.analyticsWorker.containers.image | quote }}
          imagePullPolicy: Always
          securityContext:
            # runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
          envFrom:
            - secretRef:
                name: mongodb-atlas-credentials
            - secretRef:
                name: lab-thingy-jwt-secret
            - secretRef:
                name: app-secret-key
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-worker
  labels:
    app: analytics-worker
spec:
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: analytics-worker
  type: ClusterIP

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: analytics-worker-monitor
  labels:
    release: kube-prometheus
spec:
  selector:
    matchLabels:
      app: analytics-worker
  endpoints:
    - port: http
      path: /metrics
      interval: 15s

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: orgs-service-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: analytics-worker
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
