apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-api
  labels:
    app: llm-api
spec:
  selector:
    matchLabels:
      app: llm-api
  replicas: 1
  template:
    metadata:
      labels:
        app: llm-api
    spec:
      containers:
        - name: llm-api
          image: {{ .Values.llmAPI.containers.image | quote }}
          imagePullPolicy: Always
          securityContext:
            # runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
          envFrom:
            - secretRef:
                name: mongodb-atlas-credentials
            - secretRef:
                name: app-secret-key
            - secretRef:
                name: lab-thingy-jwt-secret
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: llm-api
  labels:
    app: llm-api
spec:
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: llm-api
  type: ClusterIP

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: llm-api-monitor
  labels:
    release: kube-prometheus
spec:
  selector:
    matchLabels:
      app: llm-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: orgs-service-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: llm-api
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
